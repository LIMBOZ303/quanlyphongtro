name: Deploy Backend to Render

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Render
        run: echo "Cấu hình Render deployment thông qua Dashboard Render -> Connect GitHub repo -> Chỉ định thư mục backend"
